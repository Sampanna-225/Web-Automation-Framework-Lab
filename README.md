# Web-Automation-Framework-Lab
A hands-on lab tracking the evolution of Python web automation. The repo is split into two parts: raw Selenium scripts covering custom DOM checks, explicit waits, and raw control flow, alongside a refactored Pytest suite using reusable driver fixtures, clean assertions, and JS fallbacks for dynamic UI elements like ad overlays.
